version: '3'

services:
  db_creds:
    environment:
      - INFLUXDB=db0
      - INFLUXDB_HTTP_AUTH_ENABLED=true
      - INFLUXDB_ADMIN_USER=admin
      - INFLUXDB_ADMIN_PASSWORD=admin
      - INFLUXDB_USER=reader
      - INFLUXDB_USER_PASSWORD=reader